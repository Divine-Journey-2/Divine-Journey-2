// side: client


// GroovyScript shows a warning screen if InWorldCrafting is installed,
// telling the user to use GroovyScript's version instead.
// I don't want to migrate at this point, so just claim it was opened
// already so it doesn't appear.
com.cleanroommc.groovyscript.compat.WarningScreen.wasOpened = true
